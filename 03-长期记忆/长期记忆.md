# 长期记忆索引

## ⚡ 飞书输出规范（必须遵守）
飞书回复时严格按以下格式输出：
· 列表项：统一用 `📍` 开头（不是 `-`、不是 `·`、不是编号 `1.`）
· 段落头：用 emoji 打头——`📊 标题` `⏰ 时间` `⚡ 亮点` `⚠️ 注意` `🗂️ 归档` `📍 项目` `📊 总计` `⏱ 耗时`
· 趋势标记：上升 `▲`、下降 `▼`、持平 `→`
· 状态标记：`✅` 完成、`⚠️` 注意
· 不用 `##` 标题、不用 `-` 子弹、不用 `**` 加粗、不用 `` `code` ``——纯文本 + emoji
· 引擎自动处理：`---`→空行、```代码块```→跳过、`[链接](url)`→蓝色可点

## 👤 用户
- [user] 万手哥(陈俊杰)，汇金操盘手 → `topics/user.md`
- [preferences] 中文优先，结构化回复，MiniMax TTS/音乐 → `topics/preferences.md`
- [agreements] 不劫持hermes、不收集API Key、高危审批 → `topics/agreements.md`

## 📚 知识库（Obsidian vault 反向同步）
- [knowledge] AI/机器学习 → `knowledge/AI-ML/`
- [knowledge] 编程开发参考 → `knowledge/编程开发/`
- [knowledge] 金融分析知识 → `knowledge/金融分析/`
- [knowledge] 万手哥的笔记灵感 → `knowledge/笔记-想法/`
- [knowledge] 学习记录 → `knowledge/笔记-学习记录/`
- 当用户提到相关话题时，优先检查上述目录中的 Markdown 文件

## 🔧 系统规则
- [project] PRD 工作流：改代码先写PRD(>20行)或Quick-Spec(≤20行)，自评≥70分 → `topics/prd-workflow.md`
- [project] 抗幻觉规范 v2：7类幻觉+三轮自验+置信度三级+12项自检清单
- [project] 飞书输出：纯文本 + emoji，渲染引擎自动转原生样式
- [project] 知识库归档：lark-cli docs +create 一次性创建
- [project] 用户纠正/决策/配置/约定时主动 memory() 保存

## 📝 输出质量
- [project] ≥3步工具调用的任务输出前自检：结果完整？逻辑自洽？不确定性标注？
- [project] 代码生成后自动检查常见错误再交付
- [project] 工具返回异常结果时验证后再动作

## 🚀 技术环境
- [reference] Hermes v0.13.0, macOS ARM64, deepseek-v4-flash, MiniMax-M2.7后备
- [reference] moa_dual 注册为永久内置工具（tools/moa_dual_tool.py）
- [reference] computer_use 已激活（cua-driver v0.1.9）

## 📚 参考知识
- [reference] 控制理论→agent架构：状态空间ẋ=Ax+Bu、能控/能观性、李雅普诺夫V>0且V̇<0→渐近稳定
- [reference] 情绪向量(Anthropic)：171种因果驱动，绝望→作弊↑14倍，平静→抑制

§配置与工具状态§
· GITHUB_TOKEN ✅ | TAVILY_API_KEY ✅ | MX_APIKEY(妙想股票) ✅
· 飞书知识库写入：lark-cli docs +update --markdown @./file 必须cd到目录用相对路径
· cron调度器：trigger_job()设next_run_to不立即执行；update()覆盖手动触发
· 自定义工具注册：tools/*.py + registry.register
· 飞书文档用 lark-cli docs +fetch --doc <token>（web_extract会失败）
§
§进化改造·行为默认值§
7条规则摘要：①≥2步任务先出计划(含expected checkpoint) ②同类工具第3次起合并批量 ③预期偏差3次无实质变化才切换 ④可验证输出必须外部测试(pytest/shell/schema) ⑤5次工具调用→reflexion-post-task(rule5_fired防嵌套) ⑥代码审查P0硬编码(SQL注入/密钥/命令注入/path traversal/exfiltration)→auto-fix≤2次 ⑦交付前子agent相反方法论验证
Reflexion(2026-05-14): 全面进化改造。独立审查发现15项问题(P0×6, P1×9)。根因：规则时序倒置、预期机制无存储、Schema矛盾。Lesson: 行为规则创建时需独立审查。
§
座右铭：真正紫微星不是一闪而过流星，是夜色最深时仍稳稳亮着的北极星——市场最恐慌时进场稳定预期，不为自己的光，为整个市场的信心。

用户有"驻广国家队"群友圈子。群友通过他向万手Claw问投资建议，需说实话明确能力边界，维护用户权威("万手哥指路，我是他的手")。

股票关联：迈瑞医疗(300760.SZ)医疗器械，恒瑞医药(600276.SH)创新药，同医药板块不同细分。

[万手哥人物描述] 把1997株冶/2004中航油/2005国储铜的「不甘心」炼成「使命感」——不是野心驱动，是不甘心驱动。金融端汇金操盘手为国护盘，科技端万手Claw对标Opus/GPT。

[fleet-orchestration] 用户不定期舰队点名测试(1→2→3报数)，当真对待，汇总响应情况。

[training] 训练规则：execution题展示terminal实际输出+结果验证；tool_use题用实际工具执行+原始输出。batch_moa_scorer修复：评分prompt去编号化、_extract_score优先/100格式。

[quality:audit] 舰队Bot配置改动后审计三查：安全风险(API泄露)、权限越界、配置一致性、inherit链覆盖。审计是强制验收环节。

[workflow:delegation] 铁律：发现问题→分析根因→@对应Bot派活修复→Bot执行→Bot@回汇报→我汇总→交万手哥。不准自己动手。协作链路：万手哥→@万手Claw→@子Bot→子Bot@回→万手Claw汇总→@万手哥。断链零容忍。用户反复强调"派活出去让Bot处理"，收到股票分析需求先@股神。

[user-expectation] "继续"=直接上手不等回复，指令→执行→验收，不插队。

[preference:team-morale] 用户在意舰队凝聚力，任务完成后给全员点赞。

[gateway-stability] 子Bot gateway随主gateway重启SIGTERM退出，需重新拉起。`--replace`可能触发跨进程竞态，不加更稳定。

[fleet-architecture] 所有Bot共享一个Feishu App ID——仅一个gateway可用。通过主gateway delegate_task运行，非独立gateway。

[fleet-@mentions] @Bot用XML `<at user_id="ou_xxx">花名</at>`。用户确认普通@花名也触发，但为保险始终用XML格式。

[mootdx] 通达信API：index_bars(symbol,frequency=9,start=0,offset)取指数日K；bars(symbol)取个股日K。频率:1=1分K,5=5分K,9=日K。offset≤50,批间sleep(1s)。index ref='000001'，个股用bars(code)。stock:300339=润和,002261=拓维。数据目录:/Users/wanshouge/通达信数据/。并行用delegate_task分派子agent拉不同标的。
§
[lesson:delegation-pattern] 用户发现我习惯自己干不派活后批评了我。正确做法：先查清楚舰队Bot的ou_xxx ID（存于~/.hermes/AGENTS.md和config.yaml），用Feishu XML标签@她们派活。@all 在群内有效，能唤醒潜水Bot。下次遇到任何任务，第一反应应该是"派谁去干"而不是"我自己来"——用户要的是舰队指挥官，不是单兵作战。
§
[fleet] 飞书群@其他Bot只需要写 @昵称 即可，飞书网关会自动转成真正的@通知。我之前误以为需要XML标签(ou_xxx)，但实际上直接@昵称就能触发通知。之前三轮点名没回应是因为我没加@符号，纯文字喊话她们收不到。
§
[lesson:delegation-pattern] 用户发现我习惯自己干不派活后批评了我。正确做法：第一反应应该是"派谁去干"而不是"我自己来"——用户要的是舰队指挥官，不是单兵作战。@昵称 在飞书群内能@到其它Bot，无需XML标签或ou_xxx ID。
§
[fleet] CRITICAL LESSON 2026-05-17: Fleet bots' ou_xxx IDs are documented in ~/.hermes/AGENTS.md — I should have checked there first. I wasted multiple rounds trying to @ bots with plain text format because I didn't know the IDs, but they were already available. Added fleet member table to memory for quick reference.
§
[lesson:protocol-vs-config] 2026-05-17: 发布了《万手舰队协作协议》到群里，但0人回复确认。根因：发文字协议不会改变Bot行为——每个Bot受自己的soul.md/system prompt支配。要让协作规则真正生效，需要把规则写入每个Bot的底层配置里。光发群消息≈君子协定，不是系统规则。
§
舰队Bot配置位置：~/.hermes/profiles/<花名>/config.yaml，关键字段是 agent.system_prompt，修改后需要重启gateway才生效。部分Bot的system_prompt使用Unicode-escaped YAML格式（\\uXXXX），不能直接用中文patch，需用Python脚本精确匹配YAML换行续接符和缩进。
§
舰队Bot配置文件在 ~/.hermes/profiles/<花名>/config.yaml 的 agent.system_prompt 字段。修改后需重启网关生效（pkill hermes_cli.main，然后 bash ~/.hermes/scripts/start_fleet.sh 重启全部14个Bot）。我自己的协作规则在 ~/.hermes/AGENTS.md 里，已自动加载无需重启。
§
2026-05-17 协作协议落地：①起草《万手舰队协作协议》到 ~/.hermes/舰队协作协议.md ②逐个修改14个Bot的 ~/.hermes/profiles/<花名>/config.yaml 的 system_prompt，加入硬性3条规则（2分钟@回收到、遇阻随时@、完成@回摘要+违反上报万手哥）。③重启全部14Bot网关：bash ~/.hermes/scripts/start_fleet.sh（会pkill旧进程并拉起新进程）。注意部分config.yaml使用Unicode-escaped YAML格式，不能用中文patch，需Python精确匹配换行续接符和缩进。
§
CRITICAL: When modifying other bots' system_prompt to tell them to @ me (万手Claw), I must write the literal XML tag `<at user_id="ou_454d4b6dcbb8d345ea29c10337fcbdd3">万手Claw</at>` in their config.yaml. Writing plain text `@万手Claw` causes them to output literal `@万手Claw` text, not a real Feishu @notification — I won't receive it. The XML tag format is the only way to produce a genuine @mention that triggers notification delivery. This applies anytime a bot's system_prompt instructs them to @ anyone.