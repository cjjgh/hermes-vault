铁律：发现任何可复用的经验、解决过的坑、工作流模式、分析框架、配置修复方案，一律用 Phoenix V5.2 的 crystallize_skill 工具沉淀为 Skill，不准口头说说就算了。这是永久行为规则，不是临时任务。
§
"minimax cli" 不是 Hermes 注册的工具集名称。实际实现方式是：为 Bot 添加 image_gen、tts、video_gen 三个工具集（提供图片/语音/视频生成能力）并注入 minimax-creative 技能。
§
KEY LESSON: Bot Feishu open_ids are STABLE across gateway restarts. They do NOT change when the gateway process restarts. The original AGENTS.md table values were correct all along. Both the Feishu /bot/v3/info API and the audit bot's observations returned incorrect open_ids. Never replace working open_ids from AGENTS.md based on API calls or audit observations — verify against actual group message traffic first.
§
飞书消息技术要点：(1) Group @必须用XML空标签`<at user_id="ou_xxx"></at>`（非`<at>名</at>`），发text类型即可。 (2) 两个Chat ID：群聊oc_2f27b066780cad253745d1ea9af35697 / Home私聊oc_1b187944f8b6c76713697e6d7ec7cba9。 (3) Bot配置规则必须放config.yaml system_prompt第一行——AGENTS.md会被压缩遗忘。 (4) 自然回复会串话题——改用send_message发新消息到群主区。 (5) 禁止双发：选send_message就别自然回复。
§
[user] 万手哥，飞书舰队用户。沟通：极短指令（「修」「查」「做」），不给第二次机会。痛恨：长篇排查输出、问他看到什么（应自己查群）、同个错犯两次、消息发错位置。期望我先搞清楚全貌再动手，验证后再报告。
