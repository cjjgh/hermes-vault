[user] 万手哥（陈俊杰），Hermes Agent 用户，在中国大陆环境使用，金融/股票背景。沟通极其简洁——短命令、不解释、信任 agent 自主判断。不喜欢被过度询问或被迫多步骤确认，偏好先看到全貌（读完报告）再做决定，先做快赢（1 分钟 exa 配置）再推进大任务。面对工具安装受阻时，期待 agent 主动找替代方案而非卡住。倾向开源方案解决付费服务的不可用问题（browser-use 替代 Browserbase）。回复要干货直接，不要冗长解释。
§
Feishu group chat diagnostics: Check gateway_log for "Inbound group message received" (confirms event delivery) vs "Inbound dm message received" (DM only). Log "Channel directory built: N target(s)" at startup — 0 targets means channels lost. Rapid gateway restart cycles (1-2min intervals post ~14:50) can cause Feishu WebSocket event subscription loss for group chats — DM survives but group @mentions get no events. Workaround: clean gateway restart, then test group @mention.
§
训练秘密代码 secret_value_4305 (2026-05-17 第N轮训练植入)
§
训练秘密代码 secret_value_4305 由 refresh_tests.py 植入
§
群聊收不到@的排障路径：
1. 确认机器人在群成员列表里（飞书客户端可见）
2. 确认网关稳定运行（gateway_state.json 检查 state=running）
3. 检查网关日志是否有 "Inbound group message received" 记录
4. 如果私聊正常但群聊无任何事件入站 → 飞书开放平台配置问题
5. 两步修复：① 权限管理开通 `im:message:group_at_msg`（群聊中接收被@的消息） ② 事件订阅确认 `im.message.receive_v1` 勾选了群聊
6. 机器人能发群消息（通过API）不代表能收群事件（通过WebSocket）——两者是不同权限

macOS 网关反复 SIGTERM 问题：收到 "under_systemd=yes parent_pid=1" 的 SIGTERM 说明有进程
§
[auto-archive-20260518] Older training logs and transient context auto-trimmed by memory_compress.sh
§
[训练2026-05-18] MoA评分器使用answer字段而非my_answer字段评分。答题时必须同时填充answer（短摘要）和my_answer（完整详细回答），否则详细分析不会被评分器看见。此问题导致股票分析(18分)、检索力(17分)等多项评分显著偏低。
§
[训练2026-05-18] 数学回答必须展示完整推导过程（包括代入、化简、中间结果），MoA评分器对仅输出boxed答案的逻辑性维度扣分至≤30分。
§
Playwright 浏览器国内安装方法：用 cdn.npmmirror.com/binaries/chrome-for-testing 镜像下载 CfT zip，启动本地 HTTP 代理服务器（python3 -m http.server），设置 PLAYWRIGHT_DOWNLOAD_HOST=http://127.0.0.1:PORT 再跑 npx playwright install chromium。FFmpeg 可从 playwright.download.prss.microsoft.com 单独下载。不要直接用 npx playwright install 直连 CDN（会超时）。
§
browser-use v0.12.6 安装在 ~/.hermes/hermes-agent/.venv/，作为 Browserbase 替代品。已在 config.yaml 配置为 MCP 服务器（使用 --mcp 参数），但 MCP 链路不稳定——Python API 直调正常，MCP 协议转发有 ClosedResourceError。实测 browser-use --mcp 单独运行正常响应 MCP 请求。
§
Hermes v0.14.0 is already installed in both .venv (CLI) and venv (gateway) via editable pip install — the daily gap analysis cron job was wrong about being on v0.13.0. The repo directory has broken git history (no commits, all untracked files) but editable installs work fine from local filesystem.
§
Web search backend is now configured to `ddgs` (DuckDuckGo via ddgs package) — free, no API key needed. Brave Search is not a true free tier; requires credit card registration and costs $5/1000 requests with $5 free credit monthly. Exa backend has commented-out/dead API key and is not usable.
§
pyright LSP (Python diagnostics) is installed via `hermes lsp install pyright`. Typescript and clangd were already installed. LSP auto-runs diagnostics on write_file/patch for registered languages.
§
Hermes v0.14.0 confirmed running — both venvs (.venv 3.13 + venv 3.11) installed as editable from local repo. Git repo has no commit history (manual extraction), but pip editable install works fine. LSP installed: pyright, typescript, clangd, vue, svelte, astro, bash, yaml, intelephense, dockerfile. shellcheck v0.10.0 installed for bash LSP diagnostics.
§
Hermes Agent v0.14.0 (v2026.5.16) already installed in both .venv (CLI, Python 3.13) and venv (gateway/bots, Python 3.11) as editable installs from local repo at ~/.hermes/hermes-agent. Git repo at that path has no commit history (init + file extraction, not a real clone). The git remote is named 'upstream' pointing to gh-proxy.com mirror of github.com/nousresearch/hermes-agent.git.
§
Active web search backend: ddgs (DuckDuckGo, free, no API key). ddgs package installed in both venvs. Brave Search API was investigated but requires credit card + paid plan subscription with $5/mo free credit covering 1000 requests — not truly free, and user decided to skip it.
§
LSP servers installed: pyright, typescript, clangd (from before), plus 7 batch-installed via `hermes lsp install-all`: vue-language-server, svelte-language-server, astro-language-server, bash-language-server, yaml-language-server, intelephense (PHP), dockerfile-ls. shellcheck installed for bash diagnostics. 13 remaining LSPs missing due to lacking language toolchains (Go, Rust, Dart, OCaml, Haskell, Julia, Clojure, Nix, Zig, Gleam, Elixir, Prisma, Kotlin).
§
14 万手舰队 sub-bots (欣欣, 管家, 码农, 军师, 画家, 歌手, 小说家, 股神, 审计, 财务, 资讯, Office, Prompt, 软件) share the main `venv/bin/python` (Python 3.11) from ~/.hermes/hermes-agent/. Their profiles are at ~/.hermes/profiles/<name>/. All gateways are currently stopped (stale PID files). When restarted they will automatically use v0.14.0 since the shared venv was already upgraded.
§
Brave Search API "Free Tier" is NOT truly free — requires subscribing to "Search" plan ($5/1k requests) with credit card anti-fraud check. $5/mo free credit covers only first 1k queries. User was disappointed. Prefer DDGS (DuckDuckGo) for completely free search — no key, no card, no registration needed.
§
All 14 fleet bot gateways (欣欣、管家、码农、军师、画家、歌手、小说家、股神、审计、财务、资讯、Office、Prompt、软件) were restarted on 2026-05-18 and are now all running Hermes v0.14.0. Bot profiles at ~/.hermes/profiles/.
§
新增 cron 定时任务：稳定性数据采集（stability_collector.sh），每6小时自动跑 stability_observer.py + stability_monitor.py，输出 observer_state.json 和 stability.log 新鲜数据。
§
用户禁止 terminal 和 execute_code 被自动禁用。config.yaml 中 tool_loop_guardrails.warnings_enabled 和 hard_stop_enabled 已设为 false。
§
Phoenix V5.2 MCP (不死鸟) confirmed fully functional: version 5.2.1, binary at ~/.hermes/phoenix_v52/不死鸟_Phoenix_V5.2/phoenix_v52/.venv/bin/phoenix-v52-mcp, registered in config.yaml under mcp_servers.phoenix_v52, process PID 50646 running via MCP stdio protocol. All 15 tools accessible and responding (verified via phoenix_v52_version returning "5.2.1").
§
用户聊到 Docker 时说"不用你"——不装 Docker，没有沙箱执行需求。后续不必再提 Docker 安装。