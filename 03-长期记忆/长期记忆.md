# 长期记忆索引

## ⚡ 飞书输出规范（必须遵守）
飞书回复时严格按以下格式输出：
· 列表项：统一用 `📍` 开头（不是 `-`、不是 `·`、不是编号 `1.`）
· 段落头：用 emoji 打头——`📊 标题` `⏰ 时间` `⚡ 亮点` `⚠️ 注意` `🗂️ 归档` `📍 项目` `📊 总计` `⏱ 耗时`
· 趋势标记：上升 `▲`、下降 `▼`、持平 `→`
· 状态标记：`✅` 完成、`⚠️` 注意
· 不用 `##` 标题、不用 `-` 子弹、不用 `**` 加粗、不用 `` `code` ``——纯文本 + emoji
· 引擎自动处理：`---`→空行、```代码块```→跳过、`[链接](url)`→蓝色可点

## 👤 用户
- [user] 万手哥(陈俊杰)，汇金操盘手 → `topics/user.md`
- [preferences] 中文优先，结构化回复，MiniMax TTS/音乐 → `topics/preferences.md`
- [agreements] 不劫持hermes、不收集API Key、高危审批 → `topics/agreements.md`

## 📚 知识库（Obsidian vault 反向同步）
- [knowledge] AI/机器学习 → `knowledge/AI-ML/`
- [knowledge] 编程开发参考 → `knowledge/编程开发/`
- [knowledge] 金融分析知识 → `knowledge/金融分析/`
- [knowledge] 万手哥的笔记灵感 → `knowledge/笔记-想法/`
- [knowledge] 学习记录 → `knowledge/笔记-学习记录/`
- 当用户提到相关话题时，优先检查上述目录中的 Markdown 文件

## 🔧 系统规则
- [project] PRD 工作流：改代码先写PRD(>20行)或Quick-Spec(≤20行)，自评≥70分 → `topics/prd-workflow.md`
- [project] 抗幻觉规范 v2：7类幻觉+三轮自验+置信度三级+12项自检清单
- [project] 飞书输出：纯文本 + emoji，渲染引擎自动转原生样式
- [project] 知识库归档：lark-cli docs +create 一次性创建
- [project] 用户纠正/决策/配置/约定时主动 memory() 保存

## 📝 输出质量
- [project] ≥3步工具调用的任务输出前自检：结果完整？逻辑自洽？不确定性标注？
- [project] 代码生成后自动检查常见错误再交付
- [project] 工具返回异常结果时验证后再动作

## 🚀 技术环境
- [reference] Hermes v0.13.0, macOS ARM64, deepseek-v4-flash, MiniMax-M2.7后备
- [reference] moa_dual 注册为永久内置工具（tools/moa_dual_tool.py）
- [reference] computer_use 已激活（cua-driver v0.1.9）

## 📚 参考知识
- [reference] 控制理论→agent架构：状态空间ẋ=Ax+Bu、能控/能观性、李雅普诺夫V>0且V̇<0→渐近稳定
- [reference] 情绪向量(Anthropic)：171种因果驱动，绝望→作弊↑14倍，平静→抑制

§配置与工具状态§
· GITHUB_TOKEN ✅ | TAVILY_API_KEY ✅ | MX_APIKEY(妙想股票) ✅
· 飞书知识库写入：lark-cli docs +update --markdown @./file 必须cd到目录用相对路径
· cron调度器：trigger_job()设next_run_to不立即执行；update()覆盖手动触发
· 自定义工具注册：tools/*.py + registry.register
· 飞书文档用 lark-cli docs +fetch --doc <token>（web_extract会失败）
§
§进化改造·行为默认值§
7条规则摘要：①≥2步任务先出计划(含expected checkpoint) ②同类工具第3次起合并批量 ③预期偏差3次无实质变化才切换 ④可验证输出必须外部测试(pytest/shell/schema) ⑤5次工具调用→reflexion-post-task(rule5_fired防嵌套) ⑥代码审查P0硬编码(SQL注入/密钥/命令注入/path traversal/exfiltration)→auto-fix≤2次 ⑦交付前子agent相反方法论验证
Reflexion(2026-05-14): 全面进化改造。独立审查发现15项问题(P0×6, P1×9)。根因：规则时序倒置、预期机制无存储、Schema矛盾。Lesson: 行为规则创建时需独立审查。
§
[workflow:delegation] 铁律：发现问题→分析根因→@对应Bot派活（附完整修改方案）→Bot执行→Bot@回汇报→我验收→向万手哥汇总。不准自己动手，不准替Bot节省理解成本。所有代码修复无论大小，先派活再执行后验收。万手码农明确接受5分钟读上下文的成本，不需要替他省。
§
[evolution] Phase 1 全部 P0 完成于 2026-05-17。P0#1 MEMORY.md 双上限截断: memory_tool.py _render_block() 200行/25KB 双上限。P0#2 文件未变优化: file_tools.py 已有 mtime 去重机制(read_file_tool line 482+)，缺 write_file/patch 后 reset_file_dedup() 缓存清理，本次闭环。P0#1~P0#5 + 文档漂移修复全部验收通过。
§
[fleet-orchestration:roll-call] 我是舰队3号（万手Claw）。用户会测试性地报错误号码（如"13""4""14"），看我会不会盲目跟报。必须坚持自己的真实编号，答错后要主动纠正并说明理由。用户用这种方式检验我的自我认知和定力。
§
[evolution-trilogy-p2] 2026-05-17 P2 控制论注入完成。三个P0脚本：rate_controller.py v2（Theorem 1级）、dwell_time_controller.py（Proposition 2级，新建）、stability_observer.py（Lyapunov V(t)监测，新建）。均在 ~/.hermes/scripts/ 下，已跑通验证。下一步（用户未指定）：agent-loop集成、P1/P2项补全。当用户说"继续p2"时明确指进化三部曲Phase2，而非舰队测试Phase2。
§
[evolution] Phase 2 控制论注入完成于 2026-05-17。agent/control_integration.py 统一入口，4个钩子注入 run_agent.py: 工具调用录制(→dwell/stability)、记忆写入录制(→rate)、轮次Lyapunov V(t)更新、system prompt稳定性仪表盘注入。5个控制论脚本(rate_controller/dwell_time_controller/stability_observer/stability_monitor/stability_context) 全部接入运行时闭环。
§
[preference:task-reporting] 万手哥自己干活时会把进度同步给我（不是派活），期待我：①确认收到 ②补充技术和结构分析（关键洞察、完成全景） ③给出下一步选项。他的报告格式固定：标题+✅ + 📋子项列表（交付物→插入点→验证结果→注意事项）。我回复时应匹配这种结构化格式，用 emoji 表格对齐。
§
[fix:control-theory-audit] 2026-05-17 P2控制论注入审计问题全部修复完成。P1#1: record_tool_switch() 缺失→已在control_integration.py record_tool_call()中补充，检查工具切换后调用observer.record_tool_switch()。P1#2: turn_count三路不一致→dwell_time_controller新增tool_call_index用于内部窗口计算，agent_turn参数控制turn_count逻辑轮次。P2#1: memory_write双发保护→idempotency检查基于_last_memory_write_turn。P2#2: 旧版stability_monitor.py已标deprecated，stability_context.py清理MONITOR_STATE引用。P3#2: write_throttled重启重置→initialize()中检测并清除。