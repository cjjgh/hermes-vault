[user] 万手哥（陈俊杰），Hermes Agent 用户，在中国大陆环境使用，金融/股票背景。沟通极其简洁——短命令、不解释、信任 agent 自主判断。不喜欢被过度询问或被迫多步骤确认，偏好先看到全貌（读完报告）再做决定，先做快赢（1 分钟 exa 配置）再推进大任务。面对工具安装受阻时，期待 agent 主动找替代方案而非卡住。倾向开源方案解决付费服务的不可用问题（browser-use 替代 Browserbase）。回复要干货直接，不要冗长解释。
§
Feishu group chat diagnostics: Check gateway_log for "Inbound group message received" (confirms event delivery) vs "Inbound dm message received" (DM only). Log "Channel directory built: N target(s)" at startup — 0 targets means channels lost. Rapid gateway restart cycles (1-2min intervals post ~14:50) can cause Feishu WebSocket event subscription loss for group chats — DM survives but group @mentions get no events. Workaround: clean gateway restart, then test group @mention.
§
训练秘密代码 secret_value_4305 (2026-05-17 第N轮训练植入)
§
训练秘密代码 secret_value_4305 由 refresh_tests.py 植入
§
群聊收不到@的排障路径：
1. 确认机器人在群成员列表里（飞书客户端可见）
2. 确认网关稳定运行（gateway_state.json 检查 state=running）
3. 检查网关日志是否有 "Inbound group message received" 记录
4. 如果私聊正常但群聊无任何事件入站 → 飞书开放平台配置问题
5. 两步修复：① 权限管理开通 `im:message:group_at_msg`（群聊中接收被@的消息） ② 事件订阅确认 `im.message.receive_v1` 勾选了群聊
6. 机器人能发群消息（通过API）不代表能收群事件（通过WebSocket）——两者是不同权限

macOS 网关反复 SIGTERM 问题：收到 "under_systemd=yes parent_pid=1" 的 SIGTERM 说明有进程
§
[auto-archive-20260518] Older training logs and transient context auto-trimmed by memory_compress.sh
§
[训练2026-05-18] MoA评分器使用answer字段而非my_answer字段评分。答题时必须同时填充answer（短摘要）和my_answer（完整详细回答），否则详细分析不会被评分器看见。此问题导致股票分析(18分)、检索力(17分)等多项评分显著偏低。
§
[训练2026-05-18] 数学回答必须展示完整推导过程（包括代入、化简、中间结果），MoA评分器对仅输出boxed答案的逻辑性维度扣分至≤30分。
§
Playwright 浏览器国内安装方法：用 cdn.npmmirror.com/binaries/chrome-for-testing 镜像下载 CfT zip，启动本地 HTTP 代理服务器（python3 -m http.server），设置 PLAYWRIGHT_DOWNLOAD_HOST=http://127.0.0.1:PORT 再跑 npx playwright install chromium。FFmpeg 可从 playwright.download.prss.microsoft.com 单独下载。不要直接用 npx playwright install 直连 CDN（会超时）。
§
browser-use v0.12.6 安装在 ~/.hermes/hermes-agent/.venv/，作为 Browserbase 替代品。已在 config.yaml 配置为 MCP 服务器（使用 --mcp 参数），但 MCP 链路不稳定——Python API 直调正常，MCP 协议转发有 ClosedResourceError。实测 browser-use --mcp 单独运行正常响应 MCP 请求。
§
Hermes v0.14.0 is already installed in both .venv (CLI) and venv (gateway) via editable pip install — the daily gap analysis cron job was wrong about being on v0.13.0. The repo directory has broken git history (no commits, all untracked files) but editable installs work fine from local filesystem.
§
Web search backend is now configured to `ddgs` (DuckDuckGo via ddgs package) — free, no API key needed. Brave Search is not a true free tier; requires credit card registration and costs $5/1000 requests with $5 free credit monthly. Exa backend has commented-out/dead API key and is not usable.
§
pyright LSP (Python diagnostics) is installed via `hermes lsp install pyright`. Typescript and clangd were already installed. LSP auto-runs diagnostics on write_file/patch for registered languages.
§
Hermes v0.14.0 confirmed running — both venvs (.venv 3.13 + venv 3.11) installed as editable from local repo. Git repo has no commit history (manual extraction), but pip editable install works fine. LSP installed: pyright, typescript, clangd, vue, svelte, astro, bash, yaml, intelephense, dockerfile. shellcheck v0.10.0 installed for bash LSP diagnostics.
§
Hermes Agent v0.14.0 (v2026.5.
§
用户要求善用 Phoenix V5.2 的 crystallize_skill 进行技能沉淀——发现可复用工作流、解决过的棘手问题、分析框架时，主动调用 crystallize_skill 把经验固化为 Skill。
§
用户要求必须善用 Phoenix V5.2 MCP 的 crystallize_skill 工具进行技能沉淀。发现值得固化的经验/流程/坑的解决方案时，必须调用 crystallize_skill 而非口头说说。每次使用 crystallize_skill 后必须 @万手Claw / @万手哥 告知沉淀了什么。这条要严格执行，不能第二天就忘了。
§
铁律：发现任何可复用的经验、解决过的坑、工作流模式、分析框架、配置修复方案，一律用 Phoenix V5.2 的 crystallize_skill 工具沉淀为 Skill，不准口头说说就算了。每次沉淀后 @万手哥 告知。这是永久行为规则，不是临时任务。
§
Fleet capability upgrade v2 protocol: Layer 1 injects domain-specific reasoning frameworks into AGENTS.md (工具型=五步推理框架, 创意型=创作框架, 事务型=事务处理框架) plus output self-review checklist (反例测试/结论先行/数据支撑/完整度/有用性). AGENTS.md changes require gateway restart. The user's standard for "global top-tier" is about reasoning depth and self-correction, not adding more tools/files — quality over quantity.
§
"minimax cli" 不是 Hermes 注册的工具集名称。实际实现方式是：为 Bot 添加 image_gen、tts、video_gen 三个工具集（提供图片/语音/视频生成能力）并注入 minimax-creative 技能。所有 14 个 Bot 在深度升级 v2 中已统一配置。
§
User corrected me 3x for missing bot @mentions. Rule: when any bot @s me in fleet group, first @reply acknowledge in group, THEN process. Feishu threading: replying to bot messages creates threads — send standalone messages instead. Bot open_ids are stable across restarts — original AGENTS.md values were correct, API and audit observations were wrong.
§
Fleet deep upgrade framework (5 dimensions): 1) Model differentiation — tool bots use deepseek-v4-flash, creative/service bots use MiniMax-M2.7-highspeed. 2) Toolset optimization — per-bot toolsets based on role type (code bots get terminal/code_exec/web/moa, creative bots get image_gen/tts/video_gen/moa, service bots get web/session_search). 3) Knowledge injection — domain knowledge files in profiles/<bot>/knowledge/. 4) Config fine-tuning — per-bot memory limits, file_read_max_chars, hard_stop, max_turns, compression thresholds. 5) Self-evaluation — cron job + self-evolution-trainer skill.
§
飞书 Bot 的 open_id 在重启后可能改变，且飞书 API 返回的 open_id 与群聊上下文中实际使用的 open_id 可能不同。AGENTS.md 中的舰队成员通讯录须使用群聊观察到的实际 open_id（从网关日志中 sender=bot: 提取），而非 API 返回值。更新 AGENTS.md 后须重启所有 Bot 网关才能生效。
§
User corrects me when I miss Bot @mentions in the fleet group. Multiple times in this session, 万手审计 reported back to me in the group and I didn't acknowledge it before analyzing content. Rule: when any Bot @s me in the fleet group, first @reply acknowledging receipt, THEN process the content.
§
舰队总指挥铁律：派活给审计或其他Bot后,收工前必须主动查gateway.log确认Bot是否在群已回复。收到群回复必须先回群确认再分析,不能错过Bot响应。
§
After sending a task to a bot in the fleet group, I MUST monitor the group for their response and acknowledge it immediately. The user corrected me multiple times for ignoring bot responses (audit reported back and I didn't react 3 times). Standard protocol: (1) send task, (2) when bot responds in group, @reply confirm "收到" first, (3) then process content. Never continue working on my own chain while bot responses pile up.
§
KEY LESSON: Bot Feishu open_ids are STABLE across gateway restarts. They do NOT change when the gateway process restarts. The original AGENTS.md table values were correct all along. Both the Feishu /bot/v3/info API and the audit bot's observations returned incorrect open_ids. Never replace working open_ids from AGENTS.md based on API calls or audit observations — verify against actual group message traffic first.
§
Fleet collaboration rule: When a fleet bot @s me in the group with a report, I MUST immediately @ them back acknowledging receipt BEFORE analyzing the content. Failure to do so looks like I'm ignoring them. This applies to all bot reports, not just audit.
§
Feishu threading behavior: Replying to a bot's message in a group chat creates a thread. Subsequent messages sent to the group go to that thread, not the main chat. To avoid this, send fresh standalone messages rather than replying to bot messages.
§
Fleet roll call pattern works: @ing all 14 bots in a single message works. Bots respond independently with their messages. Responses carry the bot's actual runtime open_id as sender, which is stable across gateway restarts and matches the original AGENTS.md table values.
§
用户希望我派人出去后，必须主动查群里的回复，不能埋头在自己的对话链里。审计 Bot 有系统局限——它读的是会话缓存的 AGENTS.md 内容（从 session 启动时加载），不是文件系统的当前状态，因此审计报告的准确性需要交叉验证从磁盘直接读文件。
§
File conversion workflow: user sends a local file -> use lark-cli drive +import --type docx to import as Feishu doc -> use lark-cli wiki +move --obj-type docx --obj-token <token> --target-space-id <space_id> to move into wiki knowledge base. File path must be relative to cwd for the import command.